//TODO WHAT IS NEED FOR THIS?
//ANS: It is a type for version now why we are we using it because it tell us appVersion,buildVersion and bundleIdentifier all in strings we have created a function 
export type getUserType = {
  version: string;
};
